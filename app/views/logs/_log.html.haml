.log
  = edit_for(log)
  %p.title= title_for(log)
  = image_for(log)
  %p.name= link_to log.steward.name, steward_logs_path(log.steward)
  %p.date= log.created_at.to_formatted_s(:long_ordinal) 
  %p.story= story_for(log)
