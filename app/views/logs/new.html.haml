%h1 Create Log Entry
.new 
	.log
		=form_for(@log, :html => {:multipart => true}) do |f|
			.image
				=f.label :image
				=f.file_field :image
			.seeds
				=f.label :seeds
				=f.text_field :seeds
			.story
				=f.label :story
				=f.text_area :story
			.act
				=f.submit
