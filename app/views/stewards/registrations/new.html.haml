n form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f|
  = devise_error_messages!
  = render :partial => 'steward/registrations/form_body'
= render :partial => "devise/shared/links"
